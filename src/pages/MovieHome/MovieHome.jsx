import React from "react";
import MovieList from "../../components/MovieList/MovieList";

const MovieHome = () => {
    return (
        <main>
            <MovieList />
        </main>
    );
};

export default MovieHome;
